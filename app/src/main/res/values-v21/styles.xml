<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/main_color</item>
        <item name="android:colorPrimary">@color/main_color</item>
        
        <!-- Same, but statusBarColor has higher priority -->
        <item name="android:statusBarColor">@color/main_color</item>
        <item name="android:colorPrimaryDark">@android:color/holo_blue_bright</item>
    </style>

</resources>
